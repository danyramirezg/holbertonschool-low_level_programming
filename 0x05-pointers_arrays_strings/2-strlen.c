#include "holberton.h"
#include <stdio.h>
/**
 * _strlen - Returns the length of a string.
 * @s: Parameter char.
 *
 * Return: void
*/

int _strlen(char *s)
{
int i;
for (i = 0; s[i] !='\0'; i++)
{
}
return (i);
}
