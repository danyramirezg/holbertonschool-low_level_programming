#include "holberton.h"
#include <stdio.h>
/**
 * _strlen - Returns the length of a string.
 * @s: Parameter char.
 *
 * Return: void
*/

int _strlen(char *s)
{
	size_t strlen(const char *s);

	return (*s);
}
